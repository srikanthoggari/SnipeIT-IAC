locals {
  common_resource_name = "${var.project_name}-${var.Environment}"
  # proxy_domain         = "${var.proxy_sub_domain}.${var.domain_name}"
  # image_resize_domain  = "${var.image_resize_subdomain}.${var.proxy_sub_domain}.${var.domain_name}"
}